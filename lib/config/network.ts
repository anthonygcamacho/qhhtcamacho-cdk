import NetworkSettings from "../types/NetworkSettings"

const networkSettings: NetworkSettings = {
    account: "759536341951",
    region: "us-west-2",
    cidr: "10.1.0.0/16",
    maxAzs: 2,
}

export default networkSettings
